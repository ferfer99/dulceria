import 'package:flutter/material.dart';

class CouponsPage extends StatelessWidget {
  static String id = 'coupons_page';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('SIN CUPONES DISPONIBLES'),
      ),
    );
  }
}
